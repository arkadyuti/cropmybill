// import { fromJS } from 'immutable';
// import { makeSelectAllDealsDomain } from '../selectors';

// const selector = makeSelectAllDealsDomain();

describe('makeSelectAllDealsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
