// import { fromJS } from 'immutable';
// import { makeSelectStoresDomain } from '../selectors';

// const selector = makeSelectStoresDomain();

describe('makeSelectStoresDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
