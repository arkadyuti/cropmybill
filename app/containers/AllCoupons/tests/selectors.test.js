// import { fromJS } from 'immutable';
// import { makeSelectAllCouponsDomain } from '../selectors';

// const selector = makeSelectAllCouponsDomain();

describe('makeSelectAllCouponsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
